## 수정할 것

export a 추가안됨 (해결)

'"$USER"' "'$USER'" 오류 (해결)

handle_builtin norm 수정 (미해결)

leak (미해결)

파일 나누기 (미해결)

echo hello >> libft segfault (해결)

ambigious redirection (해결)

<<a | <<b segfault (해결)

"asdsadas" "asdsadasd" 이렇게 명령어 치면 leak남 (미해결)

<$a 할때 leak남 (extract_content) (미해결)